import React from 'react';
import TeamManager from '../../features/shared/TeamManager';

// Carrier company team (dispatchers & drivers). UI is shared with the shipper side.
export default function CarrierTeam() {
  return <TeamManager />;
}
