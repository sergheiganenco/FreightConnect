import React from 'react';
import TeamManager from '../../features/shared/TeamManager';

// Shipper company team (coordinators). UI is shared with the carrier side.
export default function ShipperTeam() {
  return <TeamManager />;
}
